import React from 'react';

import VendorPage from './pages/VendorPage';

const WeddingModule = ({ websiteData }) => {
  return <VendorPage websiteData={websiteData} />;
};

export default WeddingModule;
